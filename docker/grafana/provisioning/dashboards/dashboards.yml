apiVersion: 1
providers:
  - name: SecureTenant
    orgId: 1
    folder: SecureTenant
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
